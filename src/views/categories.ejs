<%- include('partials/header') %>

<main>

    <h1><%= title %></h1>

   <% if (
    locals.user &&
    locals.user.role_name === 'admin'
) { %>

         <a href="/new-category">
            Add New Category
    </a>

<% } %>
    <ul class="category-list">
    <% categories.forEach(category => { %>

        <li>
            <a
                href="/category/<%= category.category_id %>"
                class="category-card"
            >
                <%= category.category_name %>
            </a>
        </li>

    <% }) %>
</ul>

</main>

<%- include('partials/footer') %>